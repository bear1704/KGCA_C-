#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <conio.h>
#include <algorithm>


#ifdef TEST
void arrarr(int* arr);

int main()
{
	int iarray[] = { 1,2,3,4,5 };
	int iCount = _countof(iarray);

	int* pArray = (int*)malloc(sizeof(int) * 30);

	int iCount_ = _msize(pArray);

	arrarr(iarray);

	char* p = strcat_s()

	free(pArray);

	system("pause");
	return 0;
}

void arrarr(int* arr)
{
	std::cout << sizeof(arr[0]) << std::endl;
}


#endif